---
interface Props {
	/** @default "stretch" */
	alignment?: "center" | "stretch";
}

const { alignment = "stretch" } = Astro.props;
---

<div class={alignment === "center" ? "sm:self-center" : undefined}>
	<slot />
</div>
