---
import MailingListForm from "@/components/mailing-list-form.astro";
import MainContent from "@/components/main-content.astro";
import PageSection from "@/components/page-section.astro";
import PageTitle from "@/components/page-title.astro";
import { defaultLocale as locale } from "@/config/i18n.config";
import SimplePageLayout from "@/layouts/simple-page-layout.astro";
import { createI18n } from "@/lib/i18n";

const { t } = await createI18n(locale);

const documentTitle = t("MailingListPage.meta.title");
const title = t("MailingListPage.title");
---

<SimplePageLayout locale={locale} title={documentTitle}>
	<MainContent>
		<PageSection class="grid min-h-full place-content-center">
			<PageTitle>{title}</PageTitle>
			<MailingListForm class="[--focus-ring:black]" />
		</PageSection>
	</MainContent>
</SimplePageLayout>
